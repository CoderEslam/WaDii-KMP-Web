# Project exclude paths
/.gradle/
/build/classes/
/build/classes/kotlin/js/main/

# Firebase Admin SDK service account key — server-side secret, must never ship in the web bundle
#src/jsMain/wadii.json